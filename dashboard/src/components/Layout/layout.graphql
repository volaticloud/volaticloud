# Layout queries for navigation and summary data
query GetBotsCount {
  bots(first: 1) {
    totalCount
  }
}

query GetExchangesForLayout {
  exchanges(first: 50) {
    edges {
      node {
        id
        name
      }
    }
    totalCount
  }
}
