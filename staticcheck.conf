checks = ["all", "-ST1000", "-SA4006"]

# Disable ST1000 (package comment requirement) globally
# Most generated files don't have package comments

# Disable SA4006 (this value is never used)
# False positives for intentional reassignments like: b, err = updateBot()

["."]
# Skip internal/freqtrade entirely (OpenAPI generated code)
[["anytrade/internal/freqtrade..."]]
checks = ["-all"]