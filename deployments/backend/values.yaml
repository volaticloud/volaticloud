# Helm values for VolatiCloud Backend using Nixys Universal Chart
# Chart: nixys/nxs-universal-chart
# Repo: https://registry.nixys.io/chartrepo/public
# Docs: https://github.com/nixys/nxs-universal-chart

deployments:
  volaticloud-backend:
    replicaCount: 2
    revisionHistoryLimit: 3

    strategy:
      type: RollingUpdate
      rollingUpdate:
        maxSurge: 1
        maxUnavailable: 0

    containers:
      - name: volaticloud
        image: ghcr.io/volaticloud/volaticloud
        imagePullPolicy: Always

        ports:
          - name: http
            containerPort: 8080
            protocol: TCP

        env:
          - name: VOLATICLOUD_HOST
            value: "0.0.0.0"
          - name: VOLATICLOUD_PORT
            value: "8080"
          - name: VOLATICLOUD_MONITOR_INTERVAL
            value: "30s"
          - name: VOLATICLOUD_ETCD_ENDPOINTS
            value: "volaticloud-etcd.etcd-system.svc.cluster.local:2379"

        envsFromSecret:
          volaticloud-secrets:
            - VOLATICLOUD_DATABASE
            - VOLATICLOUD_KEYCLOAK_URL
            - VOLATICLOUD_KEYCLOAK_REALM
            - VOLATICLOUD_KEYCLOAK_CLIENT_ID
            - VOLATICLOUD_KEYCLOAK_CLIENT_SECRET
            - VOLATICLOUD_SENDGRID_API_KEY
            - VOLATICLOUD_ALERT_FROM_EMAIL
            - VOLATICLOUD_ALERT_FROM_NAME

        livenessProbe:
          httpGet:
            path: /gateway/v1/health
            port: http
          initialDelaySeconds: 60
          periodSeconds: 30
          timeoutSeconds: 5
          failureThreshold: 3

        readinessProbe:
          httpGet:
            path: /gateway/v1/health
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3

        resources:
          limits:
            cpu: 1000m
            memory: 512Mi
          requests:
            cpu: 250m
            memory: 256Mi

        securityContext:
          runAsNonRoot: true
          runAsUser: 1000
          readOnlyRootFilesystem: false
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL

    podSecurityContext:
      fsGroup: 1000
      runAsNonRoot: true
      runAsUser: 1000

    podAnnotations:
      prometheus.io/scrape: "true"
      prometheus.io/port: "8080"
      prometheus.io/path: /metrics

    podLabels:
      app: volaticloud-backend
      component: api

    affinity:
      podAntiAffinity:
        preferredDuringSchedulingIgnoredDuringExecution:
          - weight: 100
            podAffinityTerm:
              labelSelector:
                matchExpressions:
                  - key: app
                    operator: In
                    values:
                      - volaticloud-backend
              topologyKey: kubernetes.io/hostname

services:
  volaticloud-backend:
    type: ClusterIP
    ports:
      - name: http
        port: 8080
        targetPort: http
        protocol: TCP

ingresses:
  api.volaticloud.com:
    ingressClassName: nginx
    annotations:
      nginx.ingress.kubernetes.io/ssl-redirect: "true"
      nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    certManager:
      issuerName: letsencrypt-prod
      issuerType: cluster-issuer
    hosts:
      - paths:
          - path: /
            pathType: Prefix
            serviceName: volaticloud-backend
            servicePort: http

hpa:
  volaticloud-backend:
    minReplicas: 2
    maxReplicas: 10
    metrics:
      - type: Resource
        resource:
          name: cpu
          target:
            type: Utilization
            averageUtilization: 70
      - type: Resource
        resource:
          name: memory
          target:
            type: Utilization
            averageUtilization: 80

pdb:
  volaticloud-backend:
    minAvailable: 1
